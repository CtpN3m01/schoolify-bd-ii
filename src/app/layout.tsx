import metadata from './metadata';
import ClientLayout from './layout.client';

// Export metadata from a server component
export { metadata };

// Re-export the client layout as the default export
export default ClientLayout;
